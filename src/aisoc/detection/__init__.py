"""Layer 4: anomaly detection over windowed features.

Isolation Forest first (Phase 2). Autoencoder later (Phase 6, optional [deep] extra).
Scores are NOT alerts — the enrichment layer applies thresholds + corroboration.
"""
