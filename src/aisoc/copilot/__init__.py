"""Layer 6: LLM SOC copilot — explanation, severity, playbook, incident report.

Runs on a local model via Ollama (free). llm.py is the only file that knows the
runtime; swapping in a hosted API later means changing that one file.
"""
